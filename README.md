To install, add the bmp file to /Flags, and the rest of the files /Data. This should add the custom Republic of China nation to Rule the Waves 2.

Now includes the ability to play as ROC in both 1900 and 1920 scenarios.

Now includes the ability for Japan to play against the Republic of China.

To do so, add the new Japan files to /Data. Japan uses the base flag so there is no need to do anything there.
